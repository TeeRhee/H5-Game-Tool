# Component Packages

Installable React component bundles for downstream repos.

## Current package

- `h5-game-map-components`: shared map and wiki UI components plus the published build output.

## Role

Keep component source here, build artifacts under each package's `dist/`, and let tool templates in `skill-pack/` decide how those components are assembled for each page type.
